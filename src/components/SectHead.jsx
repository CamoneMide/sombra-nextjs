const SectHead = ({ text }) => {
  return (
    <div>
      <h6 className="text-[12px] md:text-[14px] text-[#2563EB] uppercase tracking-[5px] font-[700]">
        {text}
      </h6>
    </div>
  );
};

export default SectHead;
